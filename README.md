# Advanced Fabrics Examples

Examples are portable and contain no real nodes, addresses, credentials or private topology.

- `config.yaml` shows the standalone configuration contract.
- `advanced-fabric.yaml` shows an observe-only Kubernetes instance.

Examples intentionally use documentation-only hostnames and address ranges. Supply deployment data through a private values or GitOps repository.
Portable Advanced Fabrics examples
